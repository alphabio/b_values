{
  "name": "@b/types",
  "version": "0.1.0",
  "description": "Zod schemas for CSS value types and Result system",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "biome lint src",
    "format": "biome format --write src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@b/keywords": "workspace:*",
    "@b/units": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@b/typescript-config": "workspace:*"
  },
  "publishConfig": {
    "access": "public"
  }
}
